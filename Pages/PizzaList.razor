@page "/pizzas"
@using PizzaOrderApp.Models
@using PizzaOrderApp.Services
@inject PizzaService PizzaService

<PageTitle>Pizzalarımız</PageTitle>

<div class="pizza-list-header">
    <h3 class="display-5">🍕 Menü: Lezzeti Seç</h3>
    <p>Dilediğiniz pizzayı seçin, boyutunu ve malzemelerini özelleştirin.</p>
</div>

@if (pizzas == null)
{
    // Veri yüklenene kadar gösterilecek yükleme animasyonu (Ders3: Async Data Loading)
    <div class="loading-indicator">
        <p>Pizzalar yükleniyor...</p>
    </div>
}
else if (!pizzas.Any())
{
    // Hiç pizza yoksa gösterilecek mesaj
    <div class="alert alert-warning" role="alert">
        Menüde şu an için pizza bulunmamaktadır.
    </div>
}
else
{
    // Pizza kartlarını listeleme
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
        @foreach (var pizza in pizzas)
        {
            <div class="col">
                <div class="card shadow-sm h-100 pizza-card">
                    <img src="@pizza.ImageUrl" class="card-img-top" alt="@pizza.Name" />
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">@pizza.Name</h5>
                        
                        <p class="card-text text-muted small flex-grow-1">
                            İçindekiler: @string.Join(", ", pizza.Ingredients.Take(3))...
                        </p>
                        
                        <div class="d-flex justify-content-between align-items-center mt-auto pt-3 border-top">
                            <span class="price-tag h5 mb-0">
                                @pizza.BasePrice.ToString("C2")*
                            </span>
                            
                            <NavLink href="@($"/pizzas/{pizza.Id}")" 
                                class="btn btn-primary">
                                Seç ve Özelleştir
                            </NavLink>
                        </div>
                        <p class="card-text small text-right mt-1">*Başlangıç fiyatı (Küçük boy)</p>
                    </div>
                </div>
            </div>
        }
    </div>
}


@code {
    private List<Pizza>? pizzas;

    // Bileşen başlatıldığında (Initialization Phase) asenkron veri yükleme
    protected override async Task OnInitializedAsync()
    {
        // PizzaService'i kullanarak verileri çeker
        pizzas = await PizzaService.GetPizzasAsync();
    }
}